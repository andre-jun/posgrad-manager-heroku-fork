<div class="p-4 border rounded-md bg-white shadow-sm space-y-1">

  <p><strong>Aluno:</strong>
    <%= report.student&.user&.full_name || "—" %>
  </p>

  <p><strong>Data de Envio:</strong>
    <%= report.date_sent&.strftime("%d de %B, %Y") || "—" %>
  </p>

  <p><strong>Orientador:</strong>
    <%= Professor.find_by(id: report.reviewer_id)&.user&.full_name || "—" %>
  </p>

  <p><strong>Status:</strong>
    <%= report.status || "—" %>
  </p>

  <p><strong>Responsável atual:</strong>
    <%= report.owner || "—" %>
  </p>

  <p><strong>Avaliação do Orientador:</strong>
    <%= report.review_professor || "—" %>
  </p>

  <p><strong>Data da Avaliação:</strong>
    <%= report.review_date&.strftime("%d de %B, %Y") || "—" %>
  </p>

  <p><strong>Avaliação da Coordenação:</strong>
    <%= report.review_administrator || "—" %>
  </p>

  <div class="flex gap-3 mt-2">

    <!-- Visualizar respostas -->
    <%= link_to "Visualizar",
        report_answers_path(report_info_id: report.id),
        class: "px-3 py-1 bg-purple-600 text-white rounded hover:bg-purple-700" %>

    <!-- Baixar PDF -->
    <%= link_to "Baixar PDF",
        export_pdf_path(report_id: report.id, format: :pdf),
        class: "px-3 py-1 bg-orange-500 text-white rounded hover:bg-orange-600" %>
  </div>

</div>
