<div class="p-4 rounded-lg border border-gray-200 bg-gray-50 mb-4">
  <p class="text-lg font-semibold">
    Período: 
    <span class="font-normal">
      <%= report.report.semester %>o Sem. <%= report.report.year %>
    </span>
  </p>

  <p>
    Entregue em: 
    <span class="font-medium">
      <%= l(report.date_sent, format: :numeric, default: "-") %>
    </span>
  </p>

  <p>
    Sua avaliação: 
    <span class="italic text-indigo-600 dark:text-indigo-400">
      <%= report.review_professor %>
    </span>
  </p>

  <p>
    Avaliação da Coordenação: 
    <span class="italic text-teal-600 dark:text-teal-400">
      <%= report.review_administrator %>
    </span>
  </p>

  <div class="flex space-x-4">
    <%= link_to "Visualizar", report_info_path(report), data: { turbo: false }, 
          class: "px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded transition duration-300" %>
    <% if report.review_professor == 'Pendente' %>
      <%= link_to "Avaliar", professor_submit_review_report_info_path(report), data: { turbo: false }, 
            class: "px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded transition duration-300" %>
    <% end %>
  </div>
</div>
