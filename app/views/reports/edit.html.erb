<% content_for :title, "Editando relatório" %>

<div class="min-h-screen flex flex-col items-center bg-gray-50 py-12">
  <h1 class="text-3xl font-bold text-blue-600 mb-8">Editando relatório</h1>

  <div class="bg-white shadow-md rounded-lg p-8 w-full max-w-lg mb-6">
    <%= render "form", report: @report %>
  </div>

  <div>
    <%= link_to "Back to home", adm_home_path, class: "text-gray-600 hover:text-gray-800 font-semibold" %>
  </div>
</div>
