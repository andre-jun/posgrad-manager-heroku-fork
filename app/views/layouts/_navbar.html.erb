<nav class="bg-white shadow-md">
  <header class="max-w-6xl mx-auto mb-6 flex justify-between items-center">
    <div>
      NAVBAR TEMPORARIA SOMENTE PARA USO DURANTE DESENVOLVIMENTO
    </div>

    <div class="flex items-center space-x-3">
      <div>
        <% if current_user.student.present? %>
          <%= link_to student_home_path, class: "bg-white shadow-md rounded-lg px-4 py-2 hover:bg-gray-100 transition duration-200 font-medium text-gray-700 flex items-center" do %>
            Student Home
          <% end%>
        <% end %>
      </div>
      <div>
        <% if current_user.professor.present? %>
          <%= link_to professor_home_path, class: "bg-white shadow-md rounded-lg px-4 py-2 hover:bg-gray-100 transition duration-200 font-medium text-gray-700 flex items-center" do %>
            Professor Home
          <% end%>
        <% end %>
      </div>
      <div>
        <% if current_user.administrator.present? %>
          <%= link_to adm_home_path, class: "bg-white shadow-md rounded-lg px-4 py-2 hover:bg-gray-100 transition duration-200 font-medium text-gray-700 flex items-center" do %>
            Administrator Home
          <% end%>
        <% end %>
      </div>
    </div>

    <div class="text-xl font-semibold text-blue-700 flex items-center space-x-2">
      <svg class="w-8 h-8 text-blue-600" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><!-- icon --></svg>
      <span class="text-sm text-gray-500 ml-2">PPgSI EACH-USP</span>
    </div>
    <div class="text-right text-gray-600 text-sm">
      <p>Logado como: <span class="font-semibold text-blue-700"><%= current_user.name %></span></p>
      <%= link_to "Edit account", edit_user_registration_path %>
      <%= button_to 'Sair', destroy_user_session_path, method: :delete, class:"text-red-500 border border-red-500 hover:bg-red-500 hover:text-white px-3 py-1 rounded text-xs" %>
    </div>
  </header>
</nav>
