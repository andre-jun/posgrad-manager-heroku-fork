<% content_for :title, "Editar Professor" %>

<div class="min-h-screen flex flex-col items-center bg-gray-50 py-12">
  <h1 class="text-3xl font-bold text-blue-600 mb-8">Editar Professor</h1>

  <div class="bg-white shadow-md rounded-lg p-8 w-full max-w-3xl mb-6">
    <%= render "form", professor: @professor, user: @user %>
  </div>

  <div>
    <%= link_to "Voltar ao painel", adm_home_path, class: "text-gray-600 hover:text-gray-800 font-semibold" %>
  </div>
</div>
