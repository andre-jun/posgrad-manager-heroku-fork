<div class="max-w-4xl mx-auto bg-white rounded-xl border border-gray-200 p-6 space-y-4 text-gray-900 mt-4" id="historico-tab">
      <% if @student.report_infos.where.not(status: "Draft" || "Sent").any? %>
          <%= render partial: "reports/professor", collection: @student.report_infos, as: :report %>
      <% else %>
          <p class="text-gray-500 italic">Nenhum relatório avaliado ainda.</p>
      <% end %>
</div>