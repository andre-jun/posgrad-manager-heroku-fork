<div class="max-w-4xl mx-auto bg-white rounded-xl border border-gray-200 p-6 space-y-4 text-gray-900 mt-4" id="historico-tab">
  <% filtered_reports = @student.report_infos.where.not(status: ["Draft"]) %>

  <% if filtered_reports.any? %>
    <div class="divide-y divide-gray-200">
      <%= render partial: "reports/professor", collection: filtered_reports, as: :report %>
    </div>
  <% else %>
    <p class="text-gray-500 italic">Nenhum relatório ainda.</p>
  <% end %>
</div>
